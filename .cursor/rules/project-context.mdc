---
description: WanderBite product domain knowledge — vision, database schema, and business rules. Foundational context for all work in this repo.
alwaysApply: true
---

# WANDERBITE — PRODUCT & DOMAIN

> Scope of this file: WHAT the product is. Build conventions, architecture,
> routes, and coding discipline live in `conventions.mdc`. Server Action
> security invariants live in `server-actions.mdc`.

## Project Vision
Wanderbite is a gamified restaurant discovery app.
- Users ($15/mo) receive 2 random "Challenges" (restaurants) per month.
- Users can swap 1 challenge per month.
- Users get $10 off $40+ spend at assigned restaurants.
- Logic is "Safety First" (Allergies) then "Discovery" (Random assignment).

## Database Schema (Key Tables)

> ⚠️ VERIFY AND CORRECT THIS SECTION BEFORE RELYING ON IT, THEN DELETE THIS NOTE.
> The table list below appears simplified and may not match the live schema.
> It lists a single `users (id, email, role, preferences_json)` table, but:
>   - Business Rule #4 (below) references `user_profiles.dietary_flags` and
>     `user_preferences.excluded_cuisines`.
>   - The PR plan references `user_profiles.role`.
> So identity/preferences data likely lives in separate `user_profiles` and
> `user_preferences` tables, not in `users`. Open your Supabase Table Editor,
> confirm the real table/column names, and rewrite the list to match. If left
> stale, the agent will generate queries against the wrong tables/columns.

Refer to these table names exactly in SQL generation:
- `markets` (id, name, status)
- `users` (id, email, role, preferences_json)
- `restaurants` (id, market_id, cuisine_tags, lat, lon, price_tier)
- `restaurant_offers` (id, restaurant_id, discount_cents, min_spend_cents, valid_from)
- `challenges` (id, user_id, cycle_month, status, swap_count_used)
- `challenge_items` (id, challenge_id, restaurant_id, status[assigned/swapped/redeemed])
- `redemptions` (id, token_hash, status[issued/verified])

## Business Rules (Strict Enforcement)
1. **Assignment:** A user cannot be assigned a restaurant they have redeemed at in the last 6 months.
2. **Frequency:** Max 2 redemptions per restaurant per rolling 12 months.
3. **Swaps:** Only 1 swap allowed per `challenge_cycle`.
4. **Safety:** If `user_profiles.dietary_flags` contains a flag (e.g., 'peanut'), NEVER assign a restaurant with that specific `allergy_flag`. Honor `user_preferences.excluded_cuisines` for cuisine opt-outs.
