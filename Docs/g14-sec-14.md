# SEC-14 — settings that need live confirmation

These hosted Auth dashboard settings were not confirmed from code. Do not mark them done from this checkout. Local admin MFA code does not prove they are enabled.

| Setting | Status |
| --- | --- |
| Email verification | Needs live confirmation |
| Password protections | Needs live confirmation |
| Session lifetime | Needs live confirmation |
| MFA support | Needs live confirmation |

This group calls the TOTP APIs (`enroll`, `challenge`, `verify`, and `getAuthenticatorAssuranceLevel`) on admin routes. That is application behavior. It is not a reading of the hosted project's Auth settings. Supabase's public TOTP guide says the TOTP API is enabled on projects by default; this work did not open the Auth dashboard, so MFA support still needs live confirmation.
